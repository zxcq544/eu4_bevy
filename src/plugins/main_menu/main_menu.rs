use bevy::prelude::*;
pub struct MainMenuPlugin;

impl Plugin for MainMenuPlugin {
    fn build(&self, app: &mut App) {
        // app.add_systems(OnEnter(GameState::MainMenu), set_main_menu_scene);
    }
}
